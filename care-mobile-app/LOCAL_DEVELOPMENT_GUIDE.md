# دليل التشغيل المحلي واختبار التطبيق 🔧

## 📋 نظرة عامة

هذا الدليل يشرح كيف تشغل الباك اند محلياً وتربط التطبيق به للاختبار والحجز.

---

## 🎯 الخطوة 1: تشغيل الباك اند محلياً

### المتطلبات:
- Java 17+
- PostgreSQL
- Maven
- IDE (IntelliJ أو Eclipse)

### ترتيب تشغيل الخدمات:

```
1. Service Registry (Eureka) → Port 8761
2. Config Server → Port 8888  
3. Gateway Service → Port 6060
4. Auth Service → Port 6061
5. Access Management → Port 6062
6. Appointment Service → Port 6064
```

### طريقة التشغيل:

#### أ) باستخدام PowerShell Script:
```powershell
cd C:\Java\care\Code
.\start-backend.ps1  # إذا موجود
```

#### ب) يدوياً - تشغيل كل خدمة على حدة:

**1. Service Registry:**
```powershell
cd service-registry
mvn spring-boot:run
# يجب أن يعمل على: http://localhost:8761
```

**2. Config Server:**
```powershell
cd config-server
mvn spring-boot:run
# يجب أن يعمل على: http://localhost:8888
```

**3. Gateway Service:**
```powershell
cd gateway-service
mvn spring-boot:run
# يجب أن يعمل على: http://localhost:6060
```

**4. Appointment Service:**
```powershell
cd appointment-service
mvn spring-boot:run
# يجب أن يعمل على: http://localhost:6064
```

### ✅ التحقق من أن الخدمات تعمل:

افتح المتصفح وافحص:
- Eureka: http://localhost:8761
- Gateway: http://localhost:6060/actuator/health
- Appointment Service: http://localhost:6064/actuator/health
- Swagger: http://localhost:6064/swagger-ui.html

---

## 📱 الخطوة 2: إعداد التطبيق للعمل مع الباك اند المحلي

### أ) للـ Android Emulator (محاكي):

التطبيق مُعد بالفعل! يستخدم `10.0.2.2` وهو IP الخاص بالكمبيوتر من داخل المحاكي.

```dart
// في app_constants.dart (جاهز)
static const String appointmentBaseUrl = 'http://10.0.2.2:6064';
```

### ب) للجهاز الحقيقي:

1. **اكتشف IP جهازك:**
```powershell
# Windows
ipconfig
# ابحث عن: IPv4 Address (مثلاً: 192.168.1.100)
```

2. **حدث app_constants.dart:**
```dart
// استبدل 10.0.2.2 بـ IP جهازك
static const String appointmentBaseUrl = 'http://192.168.1.100:6064';
static const String authBaseUrl = 'http://192.168.1.100:6061';
```

3. **تأكد أن الجهاز والتطبيق على نفس الشبكة WiFi**

---

## 🧪 الخطوة 3: اختبار نظام الحجز

### 1. إنشاء Service Types (أنواع الخدمات) في الباك اند:

افتح Swagger: http://localhost:6064/swagger-ui.html

**أ) أنشئ Service Type:**
```http
POST /api/admin/service-types
{
  "name": "Health Services",
  "nameAr": "الخدمات الصحية",
  "code": "HEALTH",
  "isActive": true
}
```

**ب) احصل على قائمة Service Types:**
```http
GET /api/mobile/service-types/lookup
```

### 2. اختبار من التطبيق:

1. **شغل التطبيق:**
```bash
cd care-mobile-app
flutter run
```

2. **افتح صفحة البحث عن المواعيد**

3. **ستظهر:**
   - ✅ القائمة الافتراضية (إذا الباك اند ما شغال)
   - ✅ القائمة من الباك اند (إذا الباك اند شغال)

### 3. جرب حجز موعد:

1. اختر نوع الخدمة من القائمة
2. اضغط "بحث عن مواعيد"
3. اختر مركز
4. اختر وقت
5. أكد الحجز

---

## 🔍 الخطوة 4: مراقبة الطلبات والاستجابات

### استخدام Browser DevTools:

افتح: http://localhost:6064/swagger-ui.html

### استخدام Postman:

1. استورد Collection من:
   ```
   appointment-service/help/appointment-service-complete.postman_collection.json
   ```

2. اضبط Base URL: `http://localhost:6064`

### استخدام PowerShell:

```powershell
# اختبار Service Types
Invoke-WebRequest -Uri "http://localhost:6064/api/mobile/service-types/lookup" -Method GET

# اختبار Health
Invoke-WebRequest -Uri "http://localhost:6064/actuator/health" -Method GET
```

---

## 🗄️ الخطوة 5: إعداد قاعدة البيانات

### أ) إنشاء قاعدة البيانات:

```sql
CREATE DATABASE appointment_db;
```

### ب) تشغيل Migrations:

الخدمة تخلق الجداول تلقائياً عند التشغيل أول مرة.

### ج) إدخال بيانات تجريبية:

استخدم Swagger أو Postman لإنشاء:
- Service Types
- Schedules
- Organization Branches

---

## 🚀 الخطوة 6: اختبار كامل للتطبيق

### سيناريو الاختبار:

1. ✅ فتح التطبيق
2. ✅ عرض قائمة الخدمات (افتراضية أو من الباك اند)
3. ✅ اختيار خدمة
4. ✅ البحث عن المراكز
5. ✅ اختيار مركز ووقت
6. ✅ تأكيد الحجز
7. ✅ عرض المواعيد المحجوزة

---

## 📝 ملاحظات مهمة:

### 1. القائمة الافتراضية:
- ✅ تظهر فوراً بدون انتظار الباك اند
- ✅ تحتوي على 5 خدمات أساسية
- ✅ يتم تحديثها تلقائياً إذا وصل الباك اند

### 2. أخطاء شائعة:

**المشكلة:** التطبيق ما بجيب بيانات من الباك اند
**الحل:** 
- تأكد أن الباك اند شغال على البورت الصحيح
- تأكد من IP (10.0.2.2 للمحاكي، IP جهازك للجهاز الحقيقي)
- تأكد أن Firebase/Gateway تسمح بالاتصال

**المشكلة:** "Connection refused"
**الحل:**
- تأكد أن PostgreSQL شغال
- تأكد أن الخدمات مشتغلة بالترتيب الصحيح

---

## 🌐 الخطوة 7: التحضير للنشر على AWS (لاحقاً)

### ما يحتاج تعديل:

1. **تغيير URLs في app_constants.dart:**
```dart
// محلي
static const String appointmentBaseUrl = 'http://10.0.2.2:6064';

// للإنتاج (بعد النشر)
static const String appointmentBaseUrl = 'https://api.yourdomain.com/appointment';
```

2. **إضافة Environment Variables:**
```dart
// يمكن استخدام flutter_dotenv
static const String appointmentBaseUrl = 
    String.fromEnvironment('APPOINTMENT_API_URL', 
    defaultValue: 'http://10.0.2.2:6064');
```

3. **إعدادات Firebase:**
   - أضف `google-services.json` للـ Android
   - أضف `GoogleService-Info.plist` للـ iOS

4. **إعدادات Google Maps:**
   - احصل على API Key
   - أضفه في `app_constants.dart`

---

## ✅ Checklist قبل النشر:

- [ ] جميع الخدمات تعمل محلياً
- [ ] التطبيق يتصل بالباك اند بنجاح
- [ ] يمكن حجز موعد من التطبيق
- [ ] البيانات تُحفظ في قاعدة البيانات
- [ ] لا توجد أخطاء في Console
- [ ] APK يُبنى بنجاح (`flutter build apk --release`)

---

## 📞 المساعدة:

إذا واجهت مشاكل:
1. تحقق من Logs في كل خدمة
2. راجع Swagger للـ API
3. افحص قاعدة البيانات مباشرة
4. تأكد من أن Firewall لا يحجب الاتصالات

---

**آخر تحديث:** 2025-01-02

