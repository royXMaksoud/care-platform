# دليل اختبار سريع 🚀

## ⚡ اختبار سريع في 5 دقائق

### الخطوة 1: شغل الباك اند (إذا مش شغال)

```powershell
# شغل Appointment Service
cd appointment-service
mvn spring-boot:run
```

انتظر حتى يظهر: `Started AppointmentServiceApplication`

### الخطوة 2: تحقق من الخدمة

افتح المتصفح:
```
http://localhost:6064/actuator/health
```

يجب أن ترى: `{"status":"UP"}`

### الخطوة 3: أنشئ Service Type (للمرة الأولى)

افتح Swagger:
```
http://localhost:6064/swagger-ui.html
```

**أ) اذهب إلى:** `POST /api/admin/service-types`

**ب) اضغط Try it out**

**ج) أدخل:**
```json
{
  "name": "Health Checkup",
  "nameAr": "فحص طبي",
  "code": "HEALTH_CHECK",
  "isActive": true,
  "isLeaf": true
}
```

**د) اضغط Execute**

### الخطوة 4: شغل التطبيق

```powershell
cd care-mobile-app
flutter run
```

### الخطوة 5: اختبر في التطبيق

1. افتح صفحة "بحث عن موعد"
2. **القائمة يجب أن تظهر فوراً:**
   - ✅ إما القائمة الافتراضية (5 خدمات)
   - ✅ إما القائمة من الباك اند (إذا أنشأت Service Types)

3. **اختر خدمة**

4. **اضغط "بحث عن مواعيد"**

5. **إذا ظهرت مراكز → النظام يعمل! ✅**

---

## 🔍 اختبار بدون باك اند

التطبيق الآن يدعم العمل بدون باك اند:

1. **أوقف الباك اند** (إذا كان شغال)

2. **شغل التطبيق:**
   ```bash
   flutter run
   ```

3. **افتح صفحة البحث عن المواعيد**

4. **ستظهر القائمة الافتراضية فوراً** (5 خدمات)

5. **يمكنك الاختيار والاستمرار!**

---

## 📱 اختبار على جهاز حقيقي

### 1. اكتشف IP جهازك:

```powershell
ipconfig
# ابحث عن: IPv4 Address (مثلاً: 192.168.1.100)
```

### 2. حدث app_constants.dart:

```dart
// بدل من:
static const String appointmentBaseUrl = 'http://10.0.2.2:6064';

// استخدم:
static const String appointmentBaseUrl = 'http://192.168.1.100:6064';
```

### 3. شغل التطبيق على الجهاز:

```powershell
flutter run -d <device-id>
# أو
flutter install
```

### 4. تأكد أن:
- ✅ الجهاز والكمبيوتر على نفس WiFi
- ✅ الباك اند شغال على الكمبيوتر
- ✅ Firewall يسمح بالاتصالات

---

## ✅ علامات النجاح

- [ ] التطبيق يفتح بدون أخطاء
- [ ] قائمة الخدمات تظهر (افتراضية أو من الباك اند)
- [ ] يمكن اختيار خدمة
- [ ] يمكن البحث عن مواعيد
- [ ] لا توجد أخطاء في Console

---

**وقت الاختبار:** 5 دقائق ⏱️

