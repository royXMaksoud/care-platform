# 🚀 كيف تشغل وتجرب التطبيق محلياً (بدون نشر على AWS)

## 📌 ملخص سريع

**السؤال:** كيف أختبر التطبيق والحجز وأنا أعمل محلياً؟

**الجواب:** 
1. شغل الباك اند على جهازك (localhost)
2. التطبيق جاهز للاتصال بـ localhost تلقائياً
3. جرب الحجز من التطبيق

---

## ✅ الخطوات البسيطة:

### 1️⃣ شغل الباك اند على جهازك

**الخدمة المهمة:** `appointment-service` على البورت `6064`

```powershell
cd appointment-service
mvn spring-boot:run
```

**انتظر حتى يظهر:** `Started AppointmentServiceApplication`

---

### 2️⃣ تحقق إنه شغال

افتح المتصفح على:
```
http://localhost:6064/actuator/health
```

إذا ظهر `{"status":"UP"}` → الباك اند شغال! ✅

---

### 3️⃣ التطبيق جاهز للاتصال!

**المهم:** التطبيق مُعد مسبقاً للاتصال بـ localhost:

- **للمحاكي (Emulator):** يستخدم `10.0.2.2` (IP الكمبيوتر من داخل المحاكي)
- **للجهاز الحقيقي:** تحتاج تغيير IP (انظر أدناه)

**الملف:** `lib/app/core/utils/app_constants.dart`
```dart
static const String appointmentBaseUrl = 'http://10.0.2.2:6064'; // ✅ جاهز!
```

---

### 4️⃣ شغل التطبيق

```powershell
cd care-mobile-app
flutter run
```

---

### 5️⃣ جرب الحجز!

1. افتح صفحة "بحث عن موعد"
2. **القائمة تظهر فوراً:**
   - القائمة الافتراضية (5 خدمات) → إذا الباك اند مش شغال
   - القائمة من الباك اند → إذا الباك اند شغال
3. اختر خدمة
4. اضغط "بحث"
5. أكد الحجز

---

## 🔧 إذا استخدمت جهاز حقيقي (مو محاكي):

### 1. اكتشف IP جهازك:
```powershell
ipconfig
# ابحث عن: IPv4 Address (مثلاً: 192.168.1.100)
```

### 2. غيّر IP في التطبيق:

افتح: `lib/app/core/utils/app_constants.dart`

```dart
// بدل من:
static const String appointmentBaseUrl = 'http://10.0.2.2:6064';

// استخدم IP جهازك:
static const String appointmentBaseUrl = 'http://192.168.1.100:6064';
```

### 3. تأكد أن:
- ✅ الجهاز والكمبيوتر على نفس WiFi
- ✅ الباك اند شغال على الكمبيوتر

---

## 📝 كيف تعمل القوائم (Services):

### الخيار 1: من الباك اند (إذا شغال)
- التطبيق يحاول جلب القائمة من الباك اند
- إذا نجح → يستخدم القائمة الحقيقية

### الخيار 2: القائمة الافتراضية (إذا الباك اند مش شغال)
- التطبيق يستخدم قائمة ثابتة مدمجة (5 خدمات)
- تعمل فوراً بدون انتظار

**الملف:** `lib/app/data/default_services.dart`

---

## 🧪 كيف تختبر الحجز:

### الطريقة 1: من التطبيق
1. شغل التطبيق
2. اذهب لصفحة البحث عن المواعيد
3. اختر خدمة
4. ابحث → اختر مركز → أكد الحجز

### الطريقة 2: من Swagger (للتجربة فقط)
1. افتح: http://localhost:6064/swagger-ui.html
2. جرب الـ APIs مباشرة

---

## 🌐 للنشر على AWS (لاحقاً):

### ما تحتاج تغييره:

#### 1. في `app_constants.dart`:

```dart
// محلي:
static const String appointmentBaseUrl = 'http://10.0.2.2:6064';

// للإنتاج:
static const String appointmentBaseUrl = 'https://api.yourdomain.com/appointment';
```

#### 2. إعدادات إضافية:

- **Firebase:** أضف `google-services.json`
- **Google Maps:** أضف API Key
- **SSL/HTTPS:** تأكد من شهادات الأمان
- **Environment Variables:** استخدم `flutter_dotenv` لـ URLs

---

## ❓ أسئلة شائعة:

### س: ليش التطبيق ما بيجيب بيانات من الباك اند؟
**ج:** 
1. تحقق إن الباك اند شغال: `http://localhost:6064/actuator/health`
2. تحقق من IP (10.0.2.2 للمحاكي، IP جهازك للجهاز الحقيقي)
3. تحقق من Logs في الباك اند

### س: كيف أعرف إن البيانات انحفظت؟
**ج:**
1. افتح قاعدة البيانات مباشرة
2. أو استخدم Swagger لفحص المواعيد: `/api/mobile/appointments`

### س: لازم أشغل كل الخدمات (Eureka, Gateway, etc)؟
**ج:** 
- **للتجربة السريعة:** `appointment-service` كفاية
- **للاختبار الكامل:** شغل جميع الخدمات بالترتيب

---

## ✅ Checklist:

- [ ] الباك اند شغال على localhost:6064
- [ ] التطبيق يتصل بالباك اند (أو يستخدم القائمة الافتراضية)
- [ ] يمكن حجز موعد من التطبيق
- [ ] البيانات تُحفظ في قاعدة البيانات

---

## 📞 المساعدة:

- راجع: `LOCAL_DEVELOPMENT_GUIDE.md` للتفاصيل الكاملة
- راجع: `QUICK_TEST_GUIDE.md` لاختبار سريع
- فحص Swagger: http://localhost:6064/swagger-ui.html

---

**تذكر:** التطبيق الآن يعمل بدون باك اند! القائمة الافتراضية متوفرة دائماً ✅

