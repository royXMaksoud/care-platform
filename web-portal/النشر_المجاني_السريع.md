# ⚡ النشر المجاني السريع - 5 دقائق

## 🎯 حلان بسيطان:

---

## ✅ الحل 1: Ngrok (أسرع - للاختبار)

### ⏱️ الوقت: 5 دقائق

### الخطوات:

#### 1. ثبت ngrok:
```powershell
choco install ngrok
# أو: https://ngrok.com/download
```

#### 2. سجّل واحصل على token:
- https://ngrok.com/signup (مجاني)
- ثم: `ngrok config add-authtoken YOUR_TOKEN`

#### 3. شغل الباك اند:
```powershell
cd appointment-service
mvn spring-boot:run
```

#### 4. أنشئ tunnel:
```powershell
# استخدم السكريبت:
.\start-ngrok.ps1

# أو يدوياً:
ngrok http 6064
```

#### 5. انسخ الـ URL:
```
Forwarding: https://abc123.ngrok-free.app -> http://localhost:6064
```

#### 6. حدث التطبيقات:

**React** (`web-portal/.env`):
```env
VITE_API_URL=https://abc123.ngrok-free.app
```

**Mobile** (`care-mobile-app/lib/app/core/utils/app_constants.dart`):
```dart
static const String appointmentBaseUrl = 'https://abc123.ngrok-free.app';
```

#### 7. جرب من أي مكان! ✅

---

## 🌐 الحل 2: Render.com (دائم - للنشر الحقيقي)

### ⏱️ الوقت: 30 دقيقة

### الخطوات السريعة:

1. **سجّل:** https://render.com (مجاني)
2. **أنشئ PostgreSQL:** Dashboard → New → PostgreSQL
3. **أنشئ Web Service:** Dashboard → New → Web Service
4. **Connect GitHub:** ارفع الكود لـ GitHub
5. **إعدادات:**
   - Build: `./mvnw clean package`
   - Start: `java -jar target/appointment-service-*.jar`
   - Environment Variables: (من قاعدة البيانات)
6. **Create**

**ستحصل على:** `https://appointment-service.onrender.com`

---

## 📱 كيف تحدث Mobile App:

### بعد الحصول على URL (Ngrok أو Render):

```dart
// في care-mobile-app/lib/app/core/utils/app_constants.dart

class AppConstants {
  // للإنتاج (بعد النشر)
  static const String appointmentBaseUrl = 'https://YOUR_URL_HERE';
  static const String authBaseUrl = 'https://YOUR_URL_HERE';
  
  // للمحلي (للاختبار)
  // static const String appointmentBaseUrl = 'http://10.0.2.2:6064';
}
```

### أعد بناء APK:
```powershell
cd care-mobile-app
flutter build apk --release
```

---

## 🔍 كيف تعرف إنه شغال:

### 1. فحص Health:
افتح في المتصفح:
```
https://YOUR_URL_HERE/actuator/health
```

يجب أن ترى: `{"status":"UP"}`

### 2. فحص Swagger:
```
https://YOUR_URL_HERE/swagger-ui.html
```

### 3. جرب من الهاتف:
- افتح Mobile App
- اذهب لصفحة البحث عن المواعيد
- يجب أن تظهر القائمة (من الباك اند أو الافتراضية)

---

## 📋 Checklist:

- [ ] ngrok/Render جاهز
- [ ] الباك اند شغال محلياً
- [ ] Tunnel/Deployment ناجح
- [ ] React App محدث بالـ URL الجديد
- [ ] Mobile App محدث بالـ URL الجديد
- [ ] APK جديد مبني
- [ ] جربت من الهاتف ✅

---

## 💡 نصيحة:

**للاختبار السريع:** استخدم Ngrok (5 دقائق)  
**للنشر الدائم:** استخدم Render.com (مجاني)

---

## 📚 تفاصيل أكثر:

- راجع: `DEPLOY_FREE_GUIDE.md` - دليل شامل
- راجع: `NGROK_QUICK_START.md` - دليل Ngrok بالتفصيل
- استخدم: `start-ngrok.ps1` - سكريبت تلقائي

---

**وقت الإعداد:** 5 دقائق ⚡  
**الكلفة:** مجاني تماماً ✅

