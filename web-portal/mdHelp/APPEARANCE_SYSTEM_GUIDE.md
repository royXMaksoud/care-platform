# 🎨 نظام التحكم الكامل في المظهر والخطوط

## نظام متكامل للتحكم في حجم الخط وألوان القالب

---

## 📱 الميزات

### 1. التحكم في حجم الخط (4 أحجام)
- ✅ **Small** - صغير (للشاشات المزدحمة)
- ✅ **Medium** - متوسط
- ✅ **Large** - كبير (افتراضي - واضح ومريح)
- ✅ **Extra Large** - كبير جداً (لسهولة القراءة)

### 2. التحكم في ألوان القالب (5 ألوان)
- 🟣 **Default** - الأزرق البنفسجي (افتراضي)
- 🔵 **Ocean Blue** - الأزرق الهادئ
- 🟢 **Forest Green** - الأخضر الطبيعي
- 🟣 **Royal Purple** - البنفسجي الملكي
- 🟠 **Sunset Orange** - البرتقالي الدافئ

### 3. الحفظ التلقائي
- يتم حفظ جميع الإعدادات تلقائياً في `localStorage`
- تطبق فوراً على جميع صفحات المشروع
- تبقى بعد إعادة تحميل الصفحة

---

## 🚀 كيفية الاستخدام

### من واجهة المستخدم:
1. اضغط على أيقونة ⚙️ **Appearance** في شريط التنقل العلوي
2. اختر حجم الخط المناسب من القائمة
3. اختر اللون المفضل من قائمة الألوان
4. الإعدادات تُطبق فوراً!

---

## 🔧 للمطورين: استخدام النظام في الكود

### 1. استخدام أحجام الخطوط الديناميكية

```jsx
import { useAppearance } from '@/contexts/AppearanceContext'

function MyComponent() {
  const { fontClasses } = useAppearance()
  
  return (
    <div>
      {/* نص عادي */}
      <p className={fontClasses.base}>Normal text</p>
      
      {/* عنوان */}
      <h1 className={fontClasses['4xl']}>Big heading</h1>
      
      {/* نص الجدول */}
      <td className={fontClasses.table}>Table cell</td>
      
      {/* عنوان الجدول */}
      <th className={fontClasses.tableHeader}>Table header</th>
    </div>
  )
}
```

### 2. الأحجام المتاحة في `fontClasses`:

```javascript
fontClasses = {
  base:        // النص العادي
  lg:          // نص أكبر قليلاً
  xl:          // نص كبير
  '2xl':       // نص أكبر
  '3xl':       // عنوان متوسط
  '4xl':       // عنوان كبير
  table:       // محتوى الجدول
  tableHeader: // رأس الجدول
}
```

### 3. استخدام الألوان الديناميكية

```jsx
function MyComponent() {
  const { theme, themeColors } = useAppearance()
  
  return (
    <div 
      style={{ 
        backgroundColor: themeColors.primary,
        color: 'white'
      }}
    >
      Current theme: {theme}
    </div>
  )
}
```

---

## 📁 هيكل الملفات

```
src/
├── contexts/
│   └── AppearanceContext.jsx      # ⭐ Context الرئيسي
├── components/
│   └── AppearanceSettings.jsx     # 🎨 مكون الإعدادات
├── layout/
│   └── TopbarShell.jsx           # شريط التنقل مع الإعدادات
└── packages/
    └── datatable/
        └── DataTable.jsx         # ✅ الجداول تدعم الخطوط الديناميكية
```

---

## ✨ الصفحات المحدثة

### ✅ محدثة بالكامل:
- `DataTable.jsx` - جميع الجداول في المشروع
- `TopbarShell.jsx` - شريط التنقل
- `SystemList.jsx` - قائمة الأنظمة
- `SectionsList.jsx` - قائمة الأقسام
- جميع صفحات CMS

### كيفية التحديث لصفحات جديدة:
```jsx
// بدلاً من استخدام أحجام ثابتة:
<p className="text-sm">Text</p>

// استخدم:
const { fontClasses } = useAppearance()
<p className={fontClasses.base}>Text</p>
```

---

## 🎯 إضافة لون جديد

في `src/contexts/AppearanceContext.jsx`:

```javascript
export const THEMES = {
  // ... الألوان الموجودة
  
  myNewTheme: {
    name: 'My Theme Name',
    primary: 'hsl(180 100% 50%)',      // اللون الأساسي
    success: 'hsl(142 76% 36%)',       // لون النجاح
    warning: 'hsl(38 92% 50%)',        // لون التحذير
    destructive: 'hsl(0 84% 60%)'      // لون الخطر
  }
}
```

---

## 🎯 إضافة حجم خط جديد

في `src/contexts/AppearanceContext.jsx`:

```javascript
export const FONT_SIZES = {
  // ... الأحجام الموجودة
  
  xxlarge: {
    name: 'Extra Extra Large',
    base: 'text-xl',
    lg: 'text-2xl',
    xl: 'text-3xl',
    '2xl': 'text-4xl',
    '3xl': 'text-5xl',
    '4xl': 'text-6xl',
    table: 'text-xl',
    tableHeader: 'text-xl'
  }
}
```

---

## 💡 نصائح للمطورين

### ✅ افعل:
- استخدم `fontClasses` في جميع المكونات الجديدة
- اختبر المكون مع جميع أحجام الخطوط
- استخدم `table` و `tableHeader` للجداول
- احفظ الإعدادات في `localStorage`

### ❌ لا تفعل:
- لا تستخدم أحجام خطوط ثابتة (`text-sm`, `text-base`, etc.)
- لا تتجاوز النظام بقيم مخصصة إلا للضرورة
- لا تنسى إضافة `useAppearance` hook

---

## 🐛 حل المشاكل

### المشكلة: الخط لا يتغير
**الحل**: تأكد من استخدام `fontClasses` وليس أحجام ثابتة

### المشكلة: اللون لا يتطبق
**الحل**: تأكد من أن CSS variables محددة بشكل صحيح في `index.css`

### المشكلة: الإعدادات لا تحفظ
**الحل**: تأكد من أن `localStorage` يعمل في المتصفح

---

## 📊 تأثير النظام

### الصفحات المتأثرة تلقائياً:
- ✅ جميع الجداول (DataTable)
- ✅ جميع الأزرار
- ✅ جميع النصوص المحدثة
- ✅ العناوين والوصف
- ✅ pagination و search

### كيفية التحقق:
1. افتح الصفحة
2. غير حجم الخط من **Small** إلى **Extra Large**
3. يجب أن ترى الفرق فوراً في جميع النصوص

---

## 🎓 مثال شامل

```jsx
import { useAppearance } from '@/contexts/AppearanceContext'

function MyDataPage() {
  const { fontClasses, theme, themeColors } = useAppearance()
  
  return (
    <div className="p-6">
      {/* العنوان */}
      <h1 className={fontClasses['3xl']}>
        My Data Page
      </h1>
      
      {/* الوصف */}
      <p className={fontClasses.base}>
        This page uses dynamic font sizing
      </p>
      
      {/* الجدول */}
      <table>
        <thead>
          <tr>
            <th className={fontClasses.tableHeader}>
              Name
            </th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td className={fontClasses.table}>
              John Doe
            </td>
          </tr>
        </tbody>
      </table>
      
      {/* زر بلون ديناميكي */}
      <button
        style={{ backgroundColor: themeColors.primary }}
        className={fontClasses.base}
      >
        Submit
      </button>
    </div>
  )
}
```

---

**تم إنشاء هذا النظام بواسطة**: AI Assistant  
**التاريخ**: 2025  
**الإصدار**: 1.0.0  

**ملاحظة**: النظام يعمل على **جميع المشروع** بما في ذلك:
- Portal Home
- CMS Dashboard
- جميع قوائم البيانات
- جميع النماذج
- جميع الأزرار والعناصر التفاعلية

