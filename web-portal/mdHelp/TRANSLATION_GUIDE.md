# 🌍 Translation System Guide

## نظام الترجمة متعدد اللغات

هذا المشروع يدعم **4 لغات**:
- 🇬🇧 **English** (الإنجليزية)
- 🇸🇦 **العربية** (Arabic) - مع دعم RTL
- 🇫🇷 **Français** (الفرنسية)
- 🇩🇪 **Deutsch** (الألمانية)

---

## 📁 هيكل ملفات الترجمة

```
src/
├── locales/
│   ├── en/
│   │   └── translation.json  # الترجمة الإنجليزية
│   ├── ar/
│   │   └── translation.json  # الترجمة العربية
│   ├── fr/
│   │   └── translation.json  # الترجمة الفرنسية
│   └── de/
│       └── translation.json  # الترجمة الألمانية
├── i18n/
│   └── config.js              # إعدادات i18next
└── components/
    └── LanguageSwitcher.jsx   # مكون تبديل اللغة
```

---

## 🚀 كيفية الاستخدام

### 1. تبديل اللغة من الواجهة
- اضغط على أيقونة 🌍 في شريط التنقل العلوي
- اختر اللغة المطلوبة من القائمة المنسدلة
- سيتم حفظ اختيارك تلقائياً في localStorage

### 2. استخدام الترجمة في الكود

```jsx
import { useTranslation } from 'react-i18next'

function MyComponent() {
  const { t } = useTranslation()
  
  return (
    <div>
      <h1>{t('cms.title')}</h1>
      <p>{t('cms.description')}</p>
      <button>{t('common.save')}</button>
    </div>
  )
}
```

---

## ✏️ إضافة ترجمات جديدة

### الخطوة 1: إضافة المفتاح في جميع ملفات الترجمة

**`src/locales/en/translation.json`**
```json
{
  "mySection": {
    "newKey": "My new text in English"
  }
}
```

**`src/locales/ar/translation.json`**
```json
{
  "mySection": {
    "newKey": "النص الجديد بالعربية"
  }
}
```

**`src/locales/fr/translation.json`**
```json
{
  "mySection": {
    "newKey": "Mon nouveau texte en français"
  }
}
```

**`src/locales/de/translation.json`**
```json
{
  "mySection": {
    "newKey": "Mein neuer Text auf Deutsch"
  }
}
```

### الخطوة 2: استخدام المفتاح في الكود

```jsx
<p>{t('mySection.newKey')}</p>
```

---

## 🎨 دعم RTL للعربية

النظام يدعم تلقائياً **RTL** (من اليمين لليسار) للغة العربية:
- يتم تغيير اتجاه الصفحة تلقائياً عند اختيار العربية
- جميع العناصر تتكيف مع الاتجاه الجديد
- الخطوط والمسافات تتم معالجتها تلقائياً

---

## 📝 الأقسام المترجمة حالياً

### Common (مشترك)
- أزرار: Create, Edit, Delete, Save, Cancel
- حالات: Loading, Active, Inactive
- رسائل: No results found

### Navigation (التنقل)
- Portal, Home, Logout
- Content Management System

### CMS Dashboard
- جميع عناوين الوحدات (Systems, Sections, Actions, etc.)
- نصوص البحث والتصفية
- رسائل الحالات الفارغة

### Portal Home
- عناوين الأنظمة
- نصوص الصلاحيات
- رسائل البحث

---

## 🔧 الإعدادات المتقدمة

### تغيير اللغة الافتراضية

في `src/i18n/config.js`:
```javascript
i18n.init({
  fallbackLng: 'en', // غير إلى 'ar' للعربية كلغة افتراضية
  // ...
})
```

### إضافة لغة جديدة

1. أنشئ مجلد جديد في `src/locales/` (مثل `es/` للإسبانية)
2. أضف `translation.json` بنفس التركيبة
3. استورد الترجمة في `src/i18n/config.js`:
```javascript
import esTranslation from '../locales/es/translation.json'

const resources = {
  // ...
  es: { translation: esTranslation }
}

i18n.init({
  supportedLngs: ['en', 'ar', 'fr', 'de', 'es'],
  // ...
})
```
4. أضف اللغة في `LanguageSwitcher.jsx`:
```javascript
const languages = [
  // ...
  { code: 'es', name: 'Español', flag: '🇪🇸' }
]
```

---

## 💡 نصائح مهمة

### ✅ افعل:
- استخدم مفاتيح وصفية (مثل `cms.searchModules`)
- نظم الترجمات في أقسام منطقية
- أضف تعليقات للمفاتيح المعقدة
- اختبر جميع اللغات بعد التعديلات

### ❌ لا تفعل:
- لا تستخدم نصوص ثابتة مباشرة في JSX
- لا تنسى إضافة الترجمة في **جميع** اللغات
- لا تستخدم أحرف خاصة في مفاتيح الترجمة

---

## 🐛 حل المشاكل

### المشكلة: النص لا يتغير عند تبديل اللغة
**الحل**: تأكد من استخدام `{t('key')}` وليس نص ثابت

### المشكلة: اللغة لا تحفظ بعد تحديث الصفحة
**الحل**: تأكد من أن localStorage يعمل في المتصفح

### المشكلة: العربية لا تظهر بشكل صحيح
**الحل**: تأكد من أن خط UTF-8 مفعّل في `index.html`:
```html
<meta charset="UTF-8" />
```

---

## 📚 المراجع

- [i18next Documentation](https://www.i18next.com/)
- [react-i18next Documentation](https://react.i18next.com/)

---

**تم إنشاء هذا النظام بواسطة**: AI Assistant
**التاريخ**: 2025
**الإصدار**: 1.0.0

